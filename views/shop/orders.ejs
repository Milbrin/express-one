<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
            <% if (orders.length <= 0) { %>
                <h1>No order yet!</h1>
            <% } else { %>
                <ul>
                    <% orders.forEach(order => { %>
                    <li class="cart__item">
                        <h1># <%= order._id %></h1>
                        <ul>
                            <% order.items.forEach(product => { %>
                                <li><%= product.quantity %> * <%= product.title %> = <%= product.quantity*product.price %> €</li>
                            <% }); %>
                        </ul>
                    </li>
                    <% }) %>
                </ul>
            <% } %>
        </main>
        <%- include('../includes/end.ejs') %>